Billie Flow keeps this resource directory so the Swift package and Xcode app
target have an explicit resource phase. The worker runtime is installed into
~/Library/Application Support/Billie Flow and is not embedded here.
